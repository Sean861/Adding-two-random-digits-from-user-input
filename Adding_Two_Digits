two_digit_number = input("Type a two digit number: ")

# print(type(two_digit_number)) this shows that input is a str

first_digit = (two_digit_number[0]) 
second_digit = (two_digit_number[1])

# print(type(first_digit)) also shows us the class is str we need to convert to int class
#result = first_digit + second_digit this needs to be converted into an int for the calculation to occur
result = int(first_digit) + int(second_digit)

print(result)
